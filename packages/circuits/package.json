{
  "name": "@attestar/circuits",
  "version": "0.0.0",
  "private": true,
  "description": "Circom solvency (proof-of-reserves-and-liabilities) circuit for Attestar.",
  "scripts": {
    "build": "bash scripts/build.sh solvency",
    "build:test": "bash scripts/build.sh solvency_test",
    "ptau": "bash scripts/ptau.sh"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  }
}
